import 'package:flutter/material.dart';

class Findworks extends StatelessWidget {
  const Findworks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Find Works"));
  }
}
